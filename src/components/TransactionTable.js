import React from 'react';

const TransactionTable = ({ burnTransactions, coinData }) => {
  return (
    <div className="header">
      <p className="header-label">Burn Transactions</p>
    </div>
  );
};

export default TransactionTable;
